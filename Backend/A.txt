
# Important Notes 

> postgress installation (password admin) > pgadmin installation > Insert dummy data 
> Create DB with name hrms_db
> /home/ashutosh/Documents/H_F/Hackethon/AI_HACKATHON_2026/Backend/src/main/java/HrmsApplication.java  run this file 
> API point Testing 

mvn clean install
mvn spring-boot:run

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>HRMS</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>HRMS</name>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- Spring Boot Core -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- PostgreSQL -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- PGVector -->
        <dependency>
            <groupId>io.hypersistence</groupId>
            <artifactId>hypersistence-utils-hibernate-60</artifactId>
            <version>3.7.0</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.32</version>
            <optional>true</optional>
        </dependency>

        <!-- Jackson -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!-- Spring Boot Plugin -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>
</project>

Yet to Add 
 
IMPORTANT
  
AI Assistant (Chat history, Parameter + CRUD )
 
OPTIONAL
 
Job CRUD operation
 
Count (Total Candidates, Active Jobs, Selected, InProgress) Candidates -> As per week, As per Month
 
Count (Applied, Screnning, Techincal Review, HR Interview, Selected) Candidate -> As per week, As per Month
 
Role based Login, Sign-up, Forgot password, OTP service
 
Setting page (Parameter + CRUD)
 
User Profile (Parameter + CRUD)
 
Reports (Pie chart, Graph and much more)

ISSUE 

2 Names 1 emails > INVALID CASE WORKING 

1 Names 2 emails > INVALID CASE 

Show exact error why error is there while form fill up 

{
    "firstName": "Asuh",
    "lastName": "Bhoyar",
    "email": "Pjjhjmsskhh@example.com",
    "phone": "8888888888",
    "location": "Remote",
    "yearsOfExperience": 4,
    "department": "ENGINEERING",
    "skills": "Spring Boot, Postman Testing",
    "resumeUrl": "https://res.cloudinary.com/demo/sample.pdf",
    "currentCompany": "API Tester Inc",
    "currentCtc": "12 LPA",
    "education": "B.Tech CS",
    "currentStage": "APPLIED"
}